@model IEnumerable<ChiaSeDuLich.Models.InforShare>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Quản lý thông tin chia sẻ</h2>

<p>
    @Html.ActionLink("Tạo mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InforShareTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InforShareContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.InforShareSource)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InforShareTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InforShareContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.InforShareSource)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InforShareId }) |
            @Html.ActionLink("Details", "Details", new { id=item.InforShareId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InforShareId })
        </td>
    </tr>
}

</table>
